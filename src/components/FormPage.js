import React from 'react';
import PropTypes from 'prop-types';

const FormPage = props => {
    return (
        <div>
           Form page
        </div>
    );
};

FormPage.propTypes = {
    
};

export default FormPage;